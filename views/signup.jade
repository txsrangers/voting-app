extends layout

block content
  h1= title
  form(method="POST", action="/signup")
    p Email
      input(type="email", name="email")
    p Password
      input(type="password", name="password")
    p
      input(type="submit")
