extends layout

block content
    if !user
        p You must sign in first!
        br
        a(href='/signin') Sign In
    else
        h1 User List
        table
            tr
                th User
                th Email
            each user, i in userlist
                tr
                    td= user.username
                    td= user.email